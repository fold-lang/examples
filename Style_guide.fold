
-- # Style Guide

-- ## Definitions

fun calc box =
  let
    x = box.position.x,
    y = box.position.y
  in
    x * y


fun calc box =
  x * y where
    x = box.position.x,
    y = box.position.y




