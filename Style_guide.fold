
-- # Style Guide

-- ## Definitions

def calc box =
  let
    x = box.position.x,
    y = box.position.y
  in
    x * y


def calc box =
  x * y where
    x = box.position.x,
    y = box.position.y




